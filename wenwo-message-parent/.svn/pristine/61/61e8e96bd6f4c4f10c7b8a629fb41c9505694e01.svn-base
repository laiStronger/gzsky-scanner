/**
 * template component
 */
!function ($) {
  "use strict"; // jshint ;_;

  $.fn.textTemplate = function(options){
	  options = options || {};
	  $(this).load(options.url, options);
  };
  
}(window.jquery);